# Normalize text to LF in the repo. Shell scripts and systemd units must be LF
# so they work on the Linux VPS regardless of who commits from Windows.
* text=auto eol=lf

*.png binary
*.jpg binary
*.ico binary
